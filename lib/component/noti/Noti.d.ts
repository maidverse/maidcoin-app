import { DomNode } from "@hanul/skynode";
export default class Noti extends DomNode {
    private closeTimeout;
    private closing;
    constructor(title: string, message: string);
    private close;
}
//# sourceMappingURL=Noti.d.ts.map